@model VPPShop.Models.FavoriteIconViewModel
@{
    string iconColor = "text-dark";
    string badgeColor = "text-dark border border-dark";
    string spacing = "ml-2";

    if ((string)ViewBag.Size == "large")
    {
        iconColor = "text-primary";
        badgeColor = "text-secondary border border-secondary";
        spacing = "ml-3";
    }
}

<a asp-controller="Favorites" asp-action="Index" class="btn px-0 @spacing">
    <i class="fas fa-heart @iconColor"></i>
    <span class="badge favorite-count @badgeColor rounded-circle" style="padding-bottom: 2px;">
        @Model.Count
    </span>
</a>